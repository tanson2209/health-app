import Hex from "@/components/Hex";
import React, { memo } from "react";

const ListMeal = memo(() => {
  return (
    <div className="my-7">
      <Hex />
    </div>
  );
});

export default ListMeal;
